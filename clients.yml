spring:
  cloud:
    consul:
      host: localhost
      port: 8500
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include: "*" #暴露所有端点
